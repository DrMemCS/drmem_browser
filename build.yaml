targets:
  $default:
    builders:
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: drmem_browser|lib/schema/drmem.graphql
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: drmem_browser|lib/schema/drmem.graphql
